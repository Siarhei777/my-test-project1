import React from 'react';
import styles from './Settings.module.css';

const Settings = () => {
    return (
        <div className = {`size_content ${styles.settings}`}>
            Settings
        </div>
    );
}

export default Settings;